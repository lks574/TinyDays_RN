# 구현 계획

## 태스크

- [x] T1: `src/domain/insights`에 오늘 요약 타입과 순수 계산 함수를 추가한다.
- [x] T2: 수유, 수면, 기저귀, 마지막 기록 계산 테스트를 추가한다.
- [x] T3: 홈 화면 요약 영역을 실제 기록 기반 요약으로 교체한다.
- [x] T4: PR 로드맵에서 PR-09 상태를 `진행 중`으로 갱신한다.

## 구현 순서

1. 오늘 날짜 필터와 마지막 기록 계산 규칙을 도메인 함수로 만든다.
2. 종료된 수면 쌍만 총 시간에 포함하는 테스트를 작성한다.
3. 홈 화면에서 `useMemo`로 요약을 계산하고 표시한다.
4. `npm run lint`, `npm run typecheck`, `npm test`를 실행한다.

## 테스트 계획

- 오늘과 이전 날짜 기록이 섞여 있을 때 오늘 기록만 집계되는지 확인한다.
- `feeding` 기록의 횟수와 `ml` 총량이 계산되는지 확인한다.
- `sleep_start`/`sleep_end` 쌍의 횟수와 분 단위 총 시간이 계산되는지 확인한다.
- `diaper_pee`, `diaper_poop` 횟수와 마지막 기록이 계산되는지 확인한다.
