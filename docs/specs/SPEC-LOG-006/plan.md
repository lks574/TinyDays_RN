# 구현 계획

## 태스크

- [x] T1: 원격 `baby_logs` row 정규화와 로컬 스코프 변환 함수를 추가한다.
- [x] T2: 로컬/원격 기록 병합 함수를 추가하고 중복 표시 정책을 테스트한다.
- [x] T3: Supabase `baby_logs` 날짜별 조회 repository를 추가한다.
- [x] T4: 로컬 기록과 원격 기록을 함께 로드하는 서비스 함수를 추가한다.
- [x] T5: 기록 탭이 원격 조회 결과를 병합해 표시하도록 연결한다.
- [x] T6: SPEC과 아키텍처/세팅/다음 작업 문서를 업데이트한다.

## 구현 순서

1. PR-16 백업 mapping과 기존 사진 원격 조회 패턴을 확인한다.
2. 원격 row를 `BabyLog`로 변환하되 로컬 family/child scope로 맞춘다.
3. 원격 조회는 선택 날짜 범위로 제한한다.
4. 로컬 기록과 원격 기록을 병합하고 로컬 기록을 우선한다.
5. 기록 탭에서 원격 실패 상태만 짧게 표시한다.

## 테스트 계획

- 원격 row 정규화, mapping 검증, 병합 중복 제거 단위 테스트.
- 원격 session 없음, mapping 없음, 조회 실패 시 로컬 기록 유지 테스트.
- `npm run lint`, `npm run typecheck`, `npm test`.
