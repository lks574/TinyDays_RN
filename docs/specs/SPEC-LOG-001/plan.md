# 구현 계획

## 태스크

- [x] T1: 빠른 기록 액션과 임시 기록 생성 helper를 만든다.
- [x] T2: 홈 화면에 빠른 기록 버튼 그룹을 배치한다.
- [x] T3: 홈 화면 로컬 state에 최근 기록을 저장하고 타임라인으로 표시한다.
- [x] T4: 빠른 기록 helper 단위 테스트를 추가한다.
- [x] T5: PR 로드맵과 다음 작업 문서를 갱신한다.

## 구현 순서

1. `src/features/logging`에 빠른 기록 타입과 helper를 추가한다.
2. helper가 `createBabyLog`를 사용해 PR-03 모델과 일관된 기록을 만든다.
3. 홈 화면에서 버튼 탭 이벤트와 최근 타임라인 로컬 state를 연결한다.
4. 테스트와 검증 명령을 실행한다.

## 테스트 계획

- 수유 빠른 기록이 `feeding`, `quick_button`, `ml` 기본값으로 생성되는지 확인한다.
- 수면 버튼이 현재 상태에 따라 `sleep_start` 또는 `sleep_end`를 생성하는지 확인한다.
- 생성된 기록이 최신순으로 정렬 가능한지 확인한다.
