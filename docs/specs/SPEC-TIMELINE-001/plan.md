# 구현 계획

## 태스크

- [ ] T1: 날짜별 조회와 타입 필터용 순수 함수 작성
- [ ] T2: 날짜별 조회 단위 테스트 작성
- [ ] T3: 기록 탭에서 `localBabyLogRepository`를 읽고 날짜 선택 UI 연결
- [ ] T4: 타입 필터와 빈 상태 UI 연결
- [ ] T5: PR-08 문서 상태 업데이트
- [ ] T6: `lint`, `typecheck`, `test` 검증

## 구현 순서

1. `src/features/timeline`에 날짜 키, 날짜 옵션, 선택 날짜 필터, 시간순 정렬 함수를 만든다.
2. 오늘/이전 날짜 구분, 타입 필터, 빈 목록 테스트를 추가한다.
3. `app/(tabs)/logs.tsx`의 placeholder를 실제 기록 탭으로 교체한다.
4. 로드맵과 다음 작업 문서를 PR-08 진행 상태에 맞게 갱신한다.
5. 프로젝트 검증 명령을 실행한다.

## 테스트 계획

- 같은 날짜 기록만 조회되는지 검증한다.
- 최신 날짜 옵션이 먼저 표시되는지 검증한다.
- 선택 날짜 안에서 기록이 시간 오름차순으로 표시되는지 검증한다.
- 타입 필터가 `all`일 때와 개별 타입일 때를 검증한다.
