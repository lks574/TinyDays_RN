# 구현 계획

## 태스크

- [x] T1: 사진 도메인 타입과 생성 helper를 추가한다.
- [x] T2: 날짜별 사진 그룹 함수를 추가하고 단위 테스트를 작성한다.
- [x] T3: `AsyncStorage` 기반 로컬 사진 저장소를 추가하고 단위 테스트를 작성한다.
- [x] T4: `expo-image-picker`를 추가하고 앱 권한 문구를 설정한다.
- [x] T5: 사진 탭에서 현재 가족/아기 기준 목록과 업로드 진입점을 구현한다.
- [x] T6: PR-12 관련 문서를 업데이트한다.

## 구현 순서

1. 도메인 모델과 그룹 로직을 만든다.
2. 로컬 저장소를 구현한다.
3. 사진 탭 UI에서 family context와 사진 저장소를 연결한다.
4. 문서와 의존성 정보를 동기화한다.
5. `lint`, `typecheck`, `test`로 검증한다.

## 테스트 계획

- 사진 생성 시 가족, 아기, 생성자, URI, 날짜 메타데이터가 보존되는지 확인한다.
- 날짜별 그룹이 선택된 가족/아기 사진만 최신 날짜 우선으로 반환하는지 확인한다.
- 로컬 저장소가 잘못된 JSON을 빈 목록으로 복구하고 저장 결과를 최신순으로 반환하는지 확인한다.
