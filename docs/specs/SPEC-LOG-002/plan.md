# 구현 계획

## 태스크

- [x] T1: parser 결과를 임시 `BabyLog`로 확정하는 helper를 만든다.
- [x] T2: 홈 화면에 텍스트 입력과 파싱 버튼을 추가한다.
- [x] T3: 파싱 결과 확인 패널과 수정 필드를 추가한다.
- [x] T4: 확인 후 저장 시 최근 타임라인에 반영한다.
- [x] T5: 자동 저장이 일어나지 않는 단위 테스트를 추가한다.

## 구현 순서

1. `src/features/logging`에 텍스트 기록 확정 helper를 추가한다.
2. 홈 화면 state에 텍스트 입력값과 pending parsed candidate를 둔다.
3. `parseBabyLogText` 결과를 확인 UI로 렌더링한다.
4. 저장 버튼에서만 `BabyLog`를 생성해 로컬 logs에 추가한다.
5. 검증 명령을 실행한다.

## 테스트 계획

- 파서 후보가 `text` source의 `BabyLog`로 변환되는지 확인한다.
- 수정한 기록 타입, 수치, 메모가 확정 기록에 반영되는지 확인한다.
- helper가 자동 저장 기능을 갖지 않고 단일 후보만 반환하는지 확인한다.
