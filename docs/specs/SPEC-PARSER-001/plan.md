# 구현 계획

## 태스크

- [x] T1: `src/domain/parser` 모듈을 추가한다.
- [x] T2: `parseBabyLogText` 순수 함수를 구현한다.
- [x] T3: 수유, 수면, 기저귀, 체온, 목욕, 메모 테스트를 추가한다.
- [x] T4: PR 로드맵 상태를 `진행 중`으로 갱신한다.

## 구현 순서

1. 파서 결과 타입을 정의한다.
2. 공통 정규화, 시간 추출, 수치 추출 helper를 만든다.
3. 높은 우선순위부터 기록 타입별 rule을 적용한다.
4. 매칭 실패 시 `unknown` 후보를 반환한다.
5. 단위 테스트와 검증 명령을 실행한다.

## 테스트 계획

- `분유 120ml 먹었어`가 `feeding`과 `120 ml`로 파싱되는지 확인한다.
- `2시에 잠들었어`가 `sleep_start`로 파싱되는지 확인한다.
- `기저귀 소변`이 `diaper_pee`로 파싱되는지 확인한다.
- `열 37.8도`가 `temperature`와 `37.8 C`로 파싱되는지 확인한다.
- 목욕, 메모, unknown fallback을 확인한다.
