# PR 로드맵

이 문서는 TinyDays MVP를 PR 단위로 나누기 위한 기준입니다. 각 PR은 하나의 명확한 목적을 가져야 하며, 핵심 기록 루프를 먼저 검증한 뒤 저장소, 요약, 인사이트, 가족, 사진 기능으로 확장합니다.

## 진행 원칙

- PR은 가능한 한 독립적으로 리뷰하고 검증할 수 있게 작게 유지한다.
- MVP 핵심 루프인 빠른 기록, 자연어 기록, 확인, 타임라인을 먼저 완성한다.
- 서버 사이드 LLM, 공개 공유, 커머스, 커뮤니티, 고급 AI 기능은 포함하지 않는다.
- 데이터 모델, 저장소, 백엔드, 앱 세팅 결정이 바뀌면 관련 문서를 함께 업데이트한다.

## 상태 관리

각 PR 항목은 `상태`를 둔다.

- `대기`: 아직 구현을 시작하지 않았다.
- `진행 중`: 구현 또는 검증 중이며 아직 커밋하지 않았다.
- `완료`: 완료 기준을 충족했고 해당 변경이 커밋되었다.

PR 작업이 끝나도 커밋 전이면 `진행 중`으로 둔다. 완료 기준 검증 후 커밋까지 끝난 시점에만 `완료`로 변경한다.

## 우선 마일스톤

첫 번째 마일스톤은 PR-01부터 PR-06까지입니다.

이 구간의 목표는 부모가 앱에서 빠르게 기록하고, 자연어 입력을 확인 후 저장하며, 최근 타임라인에서 확인하는 핵심 흐름을 동작시키는 것입니다.

## PR-01: Expo TypeScript 앱 스캐폴딩

상태: 완료

목표: 앱 개발이 가능한 기본 프로젝트 상태를 만든다.

범위:

- Node 버전 파일을 추가한다.
- Expo + React Native + TypeScript 프로젝트를 세팅한다.
- 기본 `package.json` 스크립트를 추가한다.
- `lint`, `typecheck`, `test` 실행 기반을 마련한다.
- `docs/engineering/project-setup.md`를 업데이트한다.

완료 기준:

- Expo 앱을 로컬에서 실행할 수 있다.
- `npm run lint`를 실행할 수 있다.
- `npm run typecheck`를 실행할 수 있다.
- `npm test`를 실행할 수 있다.

## PR-02: 앱 셸과 탭 구조

상태: 완료

목표: MVP 화면 경계를 먼저 만든다.

범위:

- Expo Router 또는 React Navigation 기반 탭 구조를 만든다.
- 초기 탭을 구성한다.
  - 홈
  - 기록
  - 인사이트
  - 사진
  - 가족
- 각 화면은 placeholder 수준으로 둔다.
- 공통 레이아웃, 색상, 기본 컴포넌트를 최소로 세팅한다.

완료 기준:

- 모바일 앱에서 5개 탭을 이동할 수 있다.
- 실제 데이터 저장은 아직 포함하지 않는다.

## PR-03: `baby_logs` 도메인 모델

상태: 완료

목표: 기록 데이터의 중심 타입을 확정한다.

범위:

- `src/domain/baby-logs/`를 만든다.
- `BabyLogType`을 정의한다.
- `BabyLog` 타입을 정의한다.
- `CreateBabyLogInput` 같은 생성 입력 타입을 정의한다.
- 날짜, 시간, `source`, `confidence`, `original_text` 필드를 반영한다.
- 도메인 타입과 helper 단위 테스트를 추가한다.

완료 기준:

- React 컴포넌트 없이 도메인 타입과 helper를 테스트할 수 있다.
- 코드의 `BabyLogType`과 `docs/product/mvp-scope.md`의 초기 기록 타입이 일치한다.
- 코드의 저장 필드와 `docs/architecture/decisions.md`의 `baby_logs` 초기 필드가 충돌하지 않는다.

## PR-04: 룰 기반 자연어 파서 1차

상태: 완료

목표: 텍스트를 저장 가능한 후보 기록으로 바꾼다.

범위:

- `src/domain/parser/`를 만든다.
- 순수 함수 기반 parser를 구현한다.
- 1차 지원 기록을 구현한다.
  - 수유
  - 수면 시작
  - 수면 종료
  - 기저귀 소변
  - 기저귀 대변
  - 체온
  - 목욕
  - 메모
- 결과 타입에 `parsedLog`, `confidence`, `needsConfirmation`, `originalText`를 포함한다.
- 주요 한국어 입력 예시에 대한 단위 테스트를 추가한다.

완료 기준:

- `분유 120ml 먹었어`를 수유 기록 후보로 파싱한다.
- `2시에 잠들었어`를 수면 시작 기록 후보로 파싱한다.
- `기저귀 소변`을 기저귀 소변 기록 후보로 파싱한다.
- `열 37.8도`를 체온 기록 후보로 파싱한다.
- 파싱 결과는 저장 전 확인 흐름에 연결 가능한 형태다.

## PR-05: 빠른 기록 UI

상태: 완료

목표: 부모가 버튼으로 즉시 기록 후보를 만들 수 있게 한다.

범위:

- 홈 화면에 빠른 기록 버튼을 배치한다.
- 수유, 수면, 기저귀, 체온, 목욕, 메모 진입점을 만든다.
- 서버 저장 없이 로컬 상태 또는 임시 repository를 사용한다.
- 기록 생성 흐름의 UI 뼈대를 구성한다.

완료 기준:

- 버튼 탭으로 기록 후보를 생성할 수 있다.
- 생성된 기록이 최근 타임라인 영역에 표시된다.
- 저장소 도입은 포함하지 않는다.

## PR-06: 텍스트 기록과 파싱 확인 화면

상태: 완료

목표: 자연어 입력을 실제 UX 흐름으로 연결한다.

범위:

- 홈 화면에 텍스트 기록 입력을 만든다.
- 자연어 parser를 연결한다.
- 파싱 결과 확인 UI를 만든다.
- 사용자가 수정 또는 확인한 뒤 저장한다.
- 낮은 confidence 결과는 반드시 확인이 필요하게 한다.

완료 기준:

- 텍스트 입력, 파싱, 확인, 타임라인 반영 흐름이 동작한다.
- 파싱 결과를 사용자 확인 없이 자동 저장하지 않는다.
- 음성 입력은 아직 포함하지 않아도 된다.

## PR-07: 로컬 저장소 또는 Supabase 도입 결정

상태: 완료

목표: 기록을 앱 재시작 후에도 유지할 저장 계층을 정한다.

범위:

- 저장소 선택을 문서화한다.
- 로컬 저장이면 AsyncStorage 또는 SQLite 계층을 추가한다.
- Supabase로 가면 auth, family, `baby_logs` 최소 스키마 초안을 추가한다.
- repository interface로 UI와 저장 구현을 분리한다.

완료 기준:

- 저장소 결정이 `docs/architecture/decisions.md`에 남는다.
- 앱 재시작 후 기록이 유지되거나 Supabase 저장 흐름이 동작한다.

## PR-08: 날짜별 타임라인

상태: 완료

목표: 기록을 날짜 기준으로 조회한다.

범위:

- 기록 탭을 구현한다.
- 날짜 선택을 만든다.
- 시간순 기록 목록을 표시한다.
- 기록 타입 필터 기초를 만든다.
- 수정과 삭제는 최소 기능으로 포함하거나 별도 PR로 분리한다.

완료 기준:

- 오늘과 이전 날짜 기록을 구분해서 볼 수 있다.
- 기록 생성 흐름과 같은 데이터 소스를 사용한다.

## PR-09: 오늘 요약

상태: 완료

목표: 기록 데이터에서 부모가 바로 볼 수 있는 요약을 만든다.

범위:

- `src/domain/insights/`를 만든다.
- 오늘 수유 횟수와 총량을 계산한다.
- 오늘 수면 횟수와 총 시간을 계산한다.
- 오늘 기저귀 횟수를 계산한다.
- 마지막 기록 상태를 계산한다.
- 홈 화면 요약 영역에 연결한다.

완료 기준:

- 요약 계산은 순수 도메인 함수로 테스트할 수 있다.
- 홈에서 오늘 상태가 실제 기록 기반으로 표시된다.

## PR-10: 기본 인사이트

상태: 완료

목표: 최근 7일 기준 생활 패턴을 보여준다.

범위:

- 최근 7일 수유, 수면, 기저귀 집계를 만든다.
- 단순 평균을 계산한다.
- 다음 예상 행동은 룰 기반 안내 수준으로 제한한다.
- 인사이트 탭을 구현한다.

완료 기준:

- 머신러닝 없이 deterministic logic으로 동작한다.
- 인사이트 계산은 순수 도메인 함수로 테스트할 수 있다.

## PR-11: 가족과 아기 최소 모델

상태: 완료

목표: 실제 제품 구조에 맞게 family와 child 경계를 추가한다.

범위:

- 가족 생성 placeholder 또는 최소 플로우를 만든다.
- 아기 등록 플로우를 만든다.
- `family_id`, `child_id` 기준으로 기록을 연결한다.
- 권한은 `parent`, `family` 수준으로 제한한다.

완료 기준:

- 기록이 특정 아기에게 귀속된다.
- 복잡한 초대와 권한 체계는 포함하지 않는다.

## PR-12: 사진 업로드 진입

상태: 완료

목표: 핵심 기록 루프 이후 사진 기능을 붙인다.

범위:

- 사진 탭을 구현한다.
- 날짜별 사진 목록을 만든다.
- 업로드 진입점을 만든다.
- 저장소 선택에 따라 로컬 또는 Supabase Storage 연결을 검토한다.

완료 기준:

- 사진이 가족, 아기, 날짜 기준으로 표시된다.
- 영상 생성, 댓글, 반응은 포함하지 않는다.

## PR-13: Supabase 백엔드 스키마와 RLS 초안

상태: 완료

목표: 앱 동작을 바꾸지 않고 가족 공유, 원격 백업, 사진 원격 저장을 위한 서버 schema와 권한 기반을 만든다.

범위:

- Supabase local project 구조를 추가한다.
- `families`, `family_members`, `children`, `baby_logs`, `media_assets` migration을 작성한다.
- `family_id` 기준 RLS policy를 작성한다.
- `parent`, `family` 역할만 허용한다.
- R2 private bucket을 전제로 `media_assets`에는 object key와 metadata만 저장한다.
- local 검증용 seed 또는 RLS smoke test SQL을 추가한다.

완료 기준:

- Supabase migration 적용 시 핵심 테이블이 생성된다.
- 같은 가족 구성원만 가족, 아기, 기록, 미디어 metadata를 조회할 수 있다.
- `parent`는 같은 가족의 아기, 기록, 미디어 metadata를 생성/수정할 수 있다.
- `family`는 같은 가족 데이터를 조회할 수 있지만 write는 차단된다.
- 앱의 기존 `AsyncStorage` 기록/가족/사진 흐름은 변경하지 않는다.

## PR-14: 앱 Supabase Auth 연결

상태: 완료

목표: 원격 가족 공유를 준비하기 위해 앱에 Supabase Auth client와 세션 계층을 연결한다.

범위:

- `@supabase/supabase-js`와 환경 변수 예시를 추가한다.
- 로그인/세션 client 계층을 만든다.
- 기존 로컬 기록 저장 흐름은 유지한다.
- 원격 가족 생성과 동기화는 후속 PR로 둔다.

완료 기준:

- 앱에서 Supabase session 상태를 읽을 수 있다.
- 로그인되지 않은 상태에서도 기존 로컬 MVP 흐름은 깨지지 않는다.

## PR-15: 원격 가족과 아기 bootstrap

상태: 완료

목표: 로그인한 사용자가 원격 가족과 아기 기준 데이터를 만들 수 있게 한다.

범위:

- 현재 로컬 family context를 원격 `families`, `family_members`, `children` 구조와 매핑한다.
- 첫 `parent` 구성원 생성을 위해 authenticated `bootstrap_family` RPC를 둔다.
- 로컬 ID와 원격 UUID는 별도 local-to-remote mapping record로 유지한다.
- 초대 링크/코드의 실제 공유 UX는 별도 PR로 둔다.

완료 기준:

- 로그인한 사용자는 원격 가족, 아기, 본인 `parent` 구성원을 만들 수 있다.
- 기존 로컬 ID와 원격 UUID의 매핑 전략이 문서화된다.
- 로컬 가족/기록/사진 저장 흐름은 원격 bootstrap 실패와 무관하게 유지된다.

## PR-16: `baby_logs` 클라우드 백업 1차

상태: 대기

목표: 로컬 기록 흐름을 유지하면서 `baby_logs`를 원격으로 백업하는 첫 동기화 경로를 만든다.

범위:

- 로컬 저장을 먼저 수행한다.
- 원격 저장 실패를 재시도할 수 있는 최소 queue를 둔다.
- 충돌 해결과 다중 기기 완전 동기화는 제외한다.

완료 기준:

- 새 기록이 로컬에 즉시 저장되고 원격 `baby_logs`에도 백업된다.
- 원격 실패가 앱의 빠른 기록 흐름을 막지 않는다.

## PR-17: R2 기반 사진 원격 저장 1차

상태: 대기

목표: 가족 사진을 private R2 bucket에 저장하고 Supabase에는 metadata만 남긴다.

범위:

- R2 private bucket과 object key 규칙을 확정한다.
- signed upload/download URL 발급 경로를 만든다.
- `media_assets` 상태를 `draft`에서 `uploaded`로 갱신한다.
- 썸네일 생성 고도화는 후속 PR로 둔다.

완료 기준:

- 사진 원본은 R2에 저장된다.
- Supabase에는 object key와 metadata만 저장된다.
- 가족 구성원이 아닌 사용자는 접근 URL을 받을 수 없다.

## 추천 진행 순서

1. PR-01부터 PR-06까지 진행해 핵심 기록 루프를 검증한다.
2. PR-07에서 저장소 결정을 문서화하고 구현한다.
3. PR-08부터 PR-10까지 타임라인, 오늘 요약, 인사이트를 확장한다.
4. PR-11에서 가족과 아기 경계를 제품 구조에 맞게 정리한다.
5. PR-12에서 사진 업로드를 붙인다.
6. PR-13에서 Supabase schema/RLS를 만든다.
7. PR-14부터 PR-17까지 Auth, 원격 가족 bootstrap, 기록 백업, R2 사진 저장을 순서대로 붙인다.
